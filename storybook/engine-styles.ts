/**
 * Side-effect imports for behavior-engine CSS used by AICADS primitives in Storybook.
 * Resolved via Vite aliases in `createMainConfig` (monorepo + consumer).
 */
import 'vaul/style.css';
import 'sonner/dist/styles.css';
import './landingScroll.css';
