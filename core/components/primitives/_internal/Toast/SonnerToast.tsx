/**
 * Sonner — thin adapter over sonner toast library.
 * See [../README.md](../README.md).
 */

export { Toaster as SonnerToaster, toast as sonnerToast } from 'sonner';
export type { ExternalToast, ToasterProps as SonnerToasterProps } from 'sonner';
