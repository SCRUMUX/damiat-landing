# DEPRECATED — do not use

This folder was a **vendored copy** of `@ai-ds/core` plus DAMIAT product blocks.

It is replaced by:

- **`@ai-ds/core`** — install from [SCRUMUX/AICADS-PRO](https://github.com/SCRUMUX/AICADS-PRO) git tag
- **`../product/`** — `@damiat/product` package with DAMIAT-only blocks

The `site/` app no longer uses `file:../core`. This directory will be removed in a future cleanup after Vercel confirms the new layout.

Do **not** sync from `DAMIAT/DAMIAT` into `core/`.
