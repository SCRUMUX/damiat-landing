{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "npm install --prefix site && npm run postinstall --prefix site",
  "buildCommand": "npm run build --prefix site",
  "outputDirectory": "site/dist"
}
